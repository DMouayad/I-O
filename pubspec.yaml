name: io
description: Simple income/expense tracker
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  go_router: ^18.0.0
  intl: ^0.20.2
  local_auth: ^3.0.2
  path: ^1.9.1
  shared_preferences: ^2.5.5
  signals_flutter: ^7.1.0
  sqflite: ^2.4.3
  sqflite_common_ffi: ^2.4.2+1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
flutter:
  generate: true
  uses-material-design: true
